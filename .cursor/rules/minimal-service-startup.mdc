---
description: 最小启动服务规范（核心应用服务启动指南）
alwaysApply: true
---

# 最小启动服务

完成 Docker Compose 环境启动后，需要启动以下核心应用服务才能运行系统。

## 必需的应用服务（按顺序启动）

1. **pig-gateway** - API 网关（端口 9999）
2. **pig-auth** - 认证授权服务（端口 3000）
3. **pig-upms-biz** - 用户权限管理服务（端口 4000）

## 启动顺序说明

- **pig-gateway** 是入口，但可以在其他服务未就绪前启动
- **pig-auth** 提供 OAuth2 认证，其他服务需要它来验证 token
- **pig-upms-biz** 提供用户和权限管理，是核心业务服务

## 验证服务启动

```bash
# 查看服务注册情况（Nacos）
curl http://localhost:18849/nacos/v1/ns/instance/list?serviceName=pig-gateway

# 检查 Gateway 健康状态
curl http://localhost:9999/actuator/health

# 访问 API 文档
open http://localhost:9999/doc.html
```

## 停止服务

```bash
# 查找并停止服务进程
ps aux | grep 'spring-boot:run' | grep -v grep | awk '{print $2}' | xargs kill -9
```
