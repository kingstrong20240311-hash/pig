---
description: 开发环境管理规范（Docker Compose 环境）
alwaysApply: true
---

# 开发环境管理

项目的开发环境基于 Docker Compose，所有依赖服务都在容器中运行。

## 环境位置

开发环境配置文件：`pig-e2e-test/docker-compose.yml`

## 环境包含的服务

| 服务 | 容器名 | 端口映射 | 说明 |
|------|--------|----------|------|
| MySQL 8.0 | pig-e2e-mysql | 33307:3306 | 数据库 |
| Redis 7 | pig-e2e-redis | 36380:6379 | 缓存 |
| Kafka | pig-e2e-kafka | 9093:9093 | 消息队列 |
| Zookeeper | pig-e2e-zookeeper | 22181:2181 | Kafka 依赖 |
| Nacos 3.1.0 | pig-e2e-nacos | 18849:8848, 19849:9848 | 配置中心和服务注册 |

## 环境管理操作

### 启动开发环境

```bash
cd pig-e2e-test
docker-compose up -d
```

### 重置环境

如果需要完全重置开发环境（清空所有数据），移除相关容器：

```bash
# 停止并移除所有容器
docker-compose down

# 如果需要清理 volumes（彻底重置）
docker-compose down -v

# 重新启动
docker-compose up -d
```

### 移除特定容器

```bash
# 只移除 MySQL 容器（会触发重新初始化）
docker rm -f pig-e2e-mysql

# 只移除 Redis 容器
docker rm -f pig-e2e-redis

# 重新启动被移除的服务
docker-compose up -d
```

## 数据检查

需要检查数据时，可以直接连接数据库查询：

```bash
# 连接 MySQL
mysql -h 127.0.0.1 -P 33307 -uroot -proot

# 连接 Redis
redis-cli -h 127.0.0.1 -p 36380

# 查看 Kafka topics
docker exec pig-e2e-kafka kafka-topics --list --bootstrap-server localhost:9093
```

## 数据库初始化

MySQL 容器启动时会自动执行以下初始化脚本（按顺序）：
1. `db/pig.sql` - 主数据库
2. `db/pig_config.sql` - Nacos 配置
3. `db/pig_order.sql` - 订单模块
4. `db/vault_schema.sql` - 资金账户模块

**重置数据库**：移除 MySQL 容器后重新启动即可重新执行初始化脚本。

## 注意事项

- 所有服务都配置了健康检查，启动时会自动等待依赖服务就绪
- 默认未开启数据持久化，容器移除后数据会丢失（适合测试环境）
