---
description: 服务启动时日志写入临时文件的规范
alwaysApply: true
---

# 服务启动日志规范

当需要启动服务时，必须将服务日志写入到固定命名的临时文件中，每次启动覆盖之前的日志。

## 日志文件命名规则

服务日志文件命名格式：`{服务模块名}.log`

### 命名示例

- `pig-order-biz` 服务 → `pig-order-biz.log`

## 实现要求

1. **文件位置**：日志文件应写入临时目录
2. **覆盖模式**：每次启动时覆盖（不追加）之前的日志
3. **实时写入**：使用 `tee` 命令或重定向确保日志实时写入文件

## 启动命令示例

### 使用 Shell 后台运行

```bash
# ❌ 错误：没有记录日志
mvn spring-boot:run &

# ✅ 正确：日志写入固定文件，覆盖模式
cd pig-order/pig-order-biz
nohup mvn spring-boot:run > pig-order-biz.log 2>&1 &
```